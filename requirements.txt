# Quilvar — pinned dependencies
# Update versions deliberately, not automatically.
# Test locally before bumping.

# ── Core (all watchers + watcher_utils) ──────────────────────────────────────
anthropic==0.86.0
feedparser==6.0.12
httpx==0.28.1
python-dotenv==1.2.1
supabase==2.28.3

# ── signal_validator.py ───────────────────────────────────────────────────────
pandas==2.2.3
yfinance==1.3.0

# ── claude_reasoning.py (week 5–6, not yet active) ───────────────────────────
# openai==1.75.0
# google-generativeai==0.8.5
